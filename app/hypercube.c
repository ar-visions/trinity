
//#include <import>

//double sqrt(double);
//static int n_parts = 3;

int main(int argc, char *argv[]) {
    //A_start();
    /*
    mat4f a      = mat4f(null);
    mat4f b      = translate(mat4f(null), vec3f(1.0f, 1.0f, 1.0f));
    mat4f r      = mul(a, b);

    path    gltf_file = path("hinge.gltf");
    Model   hinge     = read(gltf_file, typeid(Model));

    trinity  t = trinity();
    window   w = window(
        trinity, t,
        title,   string("hypercube"),
        width,   800,       height, 800);
    shader   draw_shader = shader(trinity, t, vert, string("cube.vert"), frag, string("cube.frag"));
    pipeline cube = pipeline(trinity, t, w, w, shader, draw_shader, uniforms, null);
    push(w, cube);

    loop(w); */
    return  0;
}